# Status: Accepted

## Decision

Use a **uv workspace monorepo** with packages under `packages/*` and apps under `apps/*`, committed `uv.lock`, and shared dev tooling at the root.

## References

- `pyproject.toml`
